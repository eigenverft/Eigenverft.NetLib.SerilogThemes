# Workspace resources

This directory is reserved for repository-workspace resources that are not owned by one project. The initial Serilog themes repository does not require additional workspace-scoped assets.
